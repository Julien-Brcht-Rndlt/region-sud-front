import { StyledHeader } from '../styles/StyledHeader';
import Logo from './Logo';

export default function Header() {
  return (
    <StyledHeader>
      {Text}

      <Logo />
    </StyledHeader>
  );
}
